package com.leetcode.bryan.medium;

import com.leetcode.bryan.common.TreeNode;
import com.sun.source.tree.Tree;

import java.util.ArrayList;
import java.util.List;


/**
 * https://leetcode.com/explore/challenge/card/september-leetcoding-challenge-2021/636/week-1-september-1st-september-7th/3961/
 *
 */

public class UniqueBinarySearchTreesII {
    /*List<TreeNode> list = new ArrayList<>();

    public List<TreeNode> generateTrees(int n) {
        generate(null, 1, n);
    }

    private void generate(TreeNode root, int start, int end) {
        if ()
        for (int i = start; i <= end; i++) {
            TreeNode node = (root == null) ? new TreeNode(i) : root;
            generate(node, 0, i - 1);
            generate(node, i + 1, end);
        }
    }*/
}